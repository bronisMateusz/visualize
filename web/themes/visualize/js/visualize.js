/**
 * @file
 * Visualize behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.visualize = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
